{
  "scripts": {
    "dev": "npx chokidar \"assets/css/*.css\" -c \"npm run build:css\" --initial",
    "build:css": "npx postcss assets/css/main.css -o assets/css/main.bundle.css",
    "tokens": "npx kubas"
  },
  "devDependencies": {
    "chokidar": "^3.5.1",
    "kubas": "^0.3.0",
    "postcss": "^8.2.6",
    "postcss-import": "^14.0.0"
  }
}
